const fs=require("fs");const path=require("path");const root=path.resolve(__dirname,"..");const failures=[];const read=(file)=>fs.readFileSync(path.join(root,file),"utf8");const css=read("shared/sial-mobile-core.css");const core=read("shared/sial-mobile-core.js");const design=read("DESIGN.md");["navigationHistoryIndexKey","syncNavigationHistoryIndex","numericNavigationIndex","navigationInFlight","clearScreenMotionState","event.persisted","source: anchor","aria-busy","sial-navigation-source"].forEach((needle)=>{if(!core.includes(needle))failures.push("Navegacion compartida incompleta: "+needle);});["sial-header-context-in","sial-header-context-in-back","sial-header-context-out","sial-header-context-out-back",".sial-navigation-source","will-change: transform, opacity"].forEach((needle)=>{if(!css.includes(needle))failures.push("Movimiento direccional incompleto: "+needle);});["### 11.5 Contrato de transici","BFCache","La recarga y la primera apertura","reduccion de movimiento"].forEach((needle)=>{if(!design.normalize("NFD").replace(/[\u0300-\u036f]/g,"").includes(needle))failures.push("Contrato de diseno incompleto: "+needle);});[".sial-view-motion-bar","ensureMotionBar","sial-navigation-progress"].forEach((needle)=>{if(css.includes(needle)||core.includes(needle))failures.push("No se permite un loader decorativo entre vistas: "+needle);});if(failures.length){console.error(failures.join("\n"));process.exit(1);}console.log("OK contrato de transiciones entre vistas");
