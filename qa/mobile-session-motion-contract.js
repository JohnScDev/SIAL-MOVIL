const fs=require("fs");const path=require("path");const root=path.resolve(__dirname,"..");const failures=[];const read=(file)=>fs.readFileSync(path.join(root,file),"utf8");const css=read("shared/sial-mobile-core.css");const core=read("shared/sial-mobile-core.js");const design=read("DESIGN.md");const library=read("libreria/sial-mobile-library.js");[".sial-page-header-actions",".sial-session-logout:hover"].forEach((needle)=>{if(!css.includes(needle))failures.push("CSS de sesion incompleto: "+needle);});["motionApplications","ensureHeaderSessionAction","requestSignOut","dataset.sessionLogout","session-signout","clearSelectedCompany()","clearSelectedContext()"].forEach((needle)=>{if(!core.includes(needle))failures.push("API de sesion incompleta: "+needle);});["### 11.4 Mapa de aplicaci","SialMobileUI.motionApplications","conserva operaciones pendientes locales"].forEach((needle)=>{if(!design.includes(needle))failures.push("Mapa de movimiento incompleto: "+needle);});["installMotionMap","data-library-motion-map","Movimiento y aplicacion"].forEach((needle)=>{if(!library.includes(needle))failures.push("Libreria de movimiento incompleta: "+needle);});["localStorage.clear","removeItem(\"sial-mobile-workflow\")"].forEach((needle)=>{if(core.includes(needle))failures.push("Cerrar sesion no debe borrar la cola operativa: "+needle);});if(failures.length){console.error(failures.join("\n"));process.exit(1);}console.log("OK contrato de movimiento y cierre de sesion");
