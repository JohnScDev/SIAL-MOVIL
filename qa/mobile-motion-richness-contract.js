const fs=require("fs");const path=require("path");const root=path.resolve(__dirname,"..");const failures=[];const read=(file)=>fs.readFileSync(path.join(root,file),"utf8");const css=read("shared/sial-mobile-core.css");const core=read("shared/sial-mobile-core.js");const design=read("DESIGN.md");const library=read("libreria/sial-mobile-library.js");["prepareNavigationReveal","clearNavigationReveal",".slice(0, 4)","--sial-motion-order","stateFeedback","is-status-updated"].forEach((needle)=>{if(!core.includes(needle))failures.push("Riqueza de movimiento incompleta: "+needle);});["sial-content-settle","calc(var(--sial-motion-order, 0) * 24ms)","sial-status-arrive",".sial-status.is-status-updated"].forEach((needle)=>{if(!css.includes(needle))failures.push("Capa visual compartida incompleta: "+needle);});["### 11.6 Presupuesto de riqueza visual","maximo cuatro superficies","no pulsan de forma permanente"].forEach((needle)=>{const plain=design.normalize("NFD").replace(/[\u0300-\u036f]/g,"");if(!plain.includes(needle))failures.push("Presupuesto visual incompleto: "+needle);});if(!library.includes('stateFeedback: "Feedback de estado"'))failures.push("La libreria no documenta feedback de estado");if(/sial-status-arrive[^}]*infinite/.test(css))failures.push("Los estados contextuales no pueden animarse permanentemente");if(failures.length){console.error(failures.join("\n"));process.exit(1);}console.log("OK contrato de riqueza visual controlada");
