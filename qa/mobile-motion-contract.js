const fs = require("fs");const path = require("path");const root = path.resolve(__dirname, "..");const failures = [];function fail(message) {  failures.push(message);}function read(relativePath) {  return fs.readFileSync(path.join(root, relativePath), "utf8");}function forbid(source, needle, label) {  if (source.includes(needle)) fail(`${label} no debe incluir ${needle}`);}const css = read(path.join("shared", "sial-mobile-core.css"));[  "--sial-motion-fast: 160ms",  "--sial-motion-base: 220ms",  "--sial-motion-slow: 340ms",  "--sial-motion-loading: 900ms",  "--sial-motion-emphasis",  "@keyframes sial-view-motion-item",  "@keyframes sial-blocked-attempt",  "@keyframes sial-continue-sweep",  "@keyframes sial-button-spin",  ".sial-btn.is-loading",  ".sial-finca-card.is-continuing",  ".sial-otp-input.is-digit-entered"].forEach((needle) => {  if (!css.includes(needle)) fail(`CSS motion incompleto: falta ${needle}`);});const coreJs = read(path.join("shared", "sial-mobile-core.js"));[  "replayMotionState",  "is-continuing",  "is-blocked-attempt",  "is-loading"].forEach((needle) => {  if (!coreJs.includes(needle)) fail(`JS motion incompleto: falta ${needle}`);});const loginJs = read(path.join("login", "sial-mobile-login.js"));[  "is-digit-entered",  "is-blocked-attempt",  "replayMotionState"].forEach((needle) => {  if (!loginJs.includes(needle)) fail(`Login recovery motion incompleto: falta ${needle}`);});[  [css, ".sial-view-motion-bar", "CSS motion movil"],  [css, "@keyframes sial-view-motion-bar", "CSS motion movil"],  [coreJs, "ensureMotionBar", "JS motion movil"],  [coreJs, "data-sial-view-motion-bar", "JS motion movil"]].forEach(([source, needle, label]) => forbid(source, needle, label));if (failures.length) {  console.error(failures.join("\n"));  process.exit(1);}console.log("OK contrato motion SIAL movil");